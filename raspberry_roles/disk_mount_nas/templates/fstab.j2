proc            /proc           proc    defaults          0       0
PARTUUID=2c0955f0-01  /boot           vfat    defaults          0       2
PARTUUID=2c0955f0-02  /               ext4    defaults,noatime  0       1
"{{NAS_DISK_MN}}"         "{{NAS_TARGET}}"    auto     noatime      0         0
# a swapfile is not a swap partition, no line here
#   use  dphys-swapfile swap[on|off]  for that
